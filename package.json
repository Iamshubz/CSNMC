{
  "name": "csnmc",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
"scripts": {
  "dev": "tsx server.ts",
  "build": "tsc --noEmit",
  "start": "tsx server.ts"
},
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}